# 🎨 Frontend (React) — Quick Setup

## Prerequisites

Install Node.js from: https://nodejs.org/

Verify installation:
```bash
node -v
npm -v
```

## Setup & Run

```bash
cd frontend
npm install
npm start
```

Application runs on: `http://localhost:3000`

## 🌐 Alternative: GitHub Codespaces

1. Open repo → **Code** → **Create codespace**
2. Run:
   ```bash
   cd frontend
   npm install
   npm start
   ```
